spring.application.name=Organizzazione_Eventi
spring.config.import=file:env.properties

# SERVER CONFIG
server.port=3001
# DB CONFIG
spring.datasource.url=jdbc:postgresql://localhost:5432/Organizzazione_Eventi
spring.datasource.username=postgres
spring.datasource.password=1234
spring.datasource.driver=org.postgresql.Driver
# HIBERNATE CONFIG
spring.jpa.hibernate.ddl-auto=update

# JWT CONFIG
jwt.secret=${questa_e_una_chiave_super_segreta_di_almeno_32_caratteri}
# MAILGUN CONFIG
mailgun.domain=${MAILGUN_DOMAIN}
mailgun.apiKey=${MAILGUN_API_KEY}